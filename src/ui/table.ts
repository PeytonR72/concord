// Class strings for a table in a card (the Raters tab's two tables), in
// tokens only.

// The table runs to the card's edges and scrolls sideways inside it on a
// phone.
export const tableShell = '-mx-4 overflow-x-auto sm:-mx-5'

// A cell's horizontal padding by its place in the row, so the outer cells
// line up with the card's own padding. Each is one alternative: a cell takes
// exactly one.
export const firstCell = 'pr-3 pl-4 sm:pl-5'
export const innerCell = 'px-3'
export const lastCell = 'pr-4 pl-3 sm:pr-5'

export const headCell = 'pb-2 text-left align-bottom text-caption font-semibold text-ink-muted'

export const bodyCell = 'py-2.5 align-top'
