export function App() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-3 px-4">
      <h1 className="text-3xl font-semibold">Concord</h1>
      <p>
        Inter-annotator agreement in the browser tab. The file never leaves the
        tab.
      </p>
    </main>
  )
}
